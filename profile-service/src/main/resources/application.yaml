spring:
  application:
    name: ProfileService
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: 12345678

server:
  port: 9002
  servlet:
    context-path: /profiles

app:
  services:
    identity: http://localhost:9001/identity